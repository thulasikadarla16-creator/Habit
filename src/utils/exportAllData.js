// Firebase-based export function
function exportAllData() {
	// For now, just show a message that data is stored in Firebase
	alert('Your data is automatically backed up to Firebase! No need to manually export.');
}

export default exportAllData;