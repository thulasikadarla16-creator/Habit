function importAllData() {
	// Firebase-based import - for now just show a message
	alert('Data import is not available with Firebase storage. Your data is automatically synced across devices!');
}

export default importAllData;